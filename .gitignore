# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
htmlcov/

# uv
.uv/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Project data (never commit large media/model/derived artifacts)
input_videos/*
!input_videos/put_input_video_here.txt
models/*
!models/put_models_here.txt
output/
output_videos/
outputs/
data/
stubs/
docker_output/
*.mp4
*.avi
*.mov
*.pt
*.pkl

# Notebook checkpoints
.ipynb_checkpoints/

# Local-only internal notes -- never publish (see the file itself for why).
# Previously kept out of git purely by never `git add`ing it, which a stray
# `git add -A`/`git add .` could silently break; ignored explicitly instead.
docs/LEARNING_GUIDE.md

# Ad-hoc local review/debug configs (e.g. configs/review_soccernet3_jersey.yaml,
# committed by accident 2026-08-03 -- referenced a training-run name that only
# exists on one machine, meaningless to anyone else cloning this repo).
configs/review_*.yaml
